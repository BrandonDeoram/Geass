import 'package:flutter/material.dart';

class AnimeCards extends StatelessWidget {
  const AnimeCards({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
