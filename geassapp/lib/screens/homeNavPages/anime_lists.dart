import 'package:flutter/material.dart';

class AnimeLists extends StatelessWidget {
  const AnimeLists({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Anime Lists"),
    );
  }
}
